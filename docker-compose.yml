version:  ' 3.8'
#define the services (our containers)
services:
  mongodb:
     image: mongo
     container_name: mongodb-microservice
     ports:
      - "27017:27017" 
     restart: always
     environment:
       MONGO_INITDB_ROOT_USERNAME: root
       MONGO_INITDB_ROOT_PASSWORD: root

